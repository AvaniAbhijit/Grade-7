import pygame #import pygame module
pygame.init() #to intiialize all the imported pygame modules

# Set up the drawing window using width and height
window = pygame.display.set_mode([500, 500]) #screen just flashes
